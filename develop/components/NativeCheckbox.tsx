import React from 'react';
import { TouchableOpacity, View } from 'react-native';

export const NativeCheckbox = () => {
  return <TouchableOpacity><View style={{width: 20, height: 20, borderWidth: 1}} /></TouchableOpacity>;
};
