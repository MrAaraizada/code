import React from 'react';
import { TouchableOpacity, View } from 'react-native';

export const NativeRadioButtons = () => {
  return <TouchableOpacity><View style={{width: 20, height: 20, borderRadius: 10, borderWidth: 1}} /></TouchableOpacity>;
};
